import React from "react";

// what parts did I need here? Is it wise to just import everything? I think I could have done it with { abc, 123 }
import * from "../Grid";
import * from "../Panel";
import * * from "../List";

export default const Layout = props =>
	